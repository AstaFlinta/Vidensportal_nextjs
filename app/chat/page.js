import ChatInput from './ChatInput'; // Import the ChatInput component

export default function Chat() {
    return (
        <div>
            <h1></h1> {/* Title for your chat interface */}
            <ChatInput /> {/* Use the ChatInput component */}
        </div>
    );
}
